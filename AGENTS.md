# AGENTS.md

- このリポジトリは静的なHTML/CSS/JavaScriptアプリとして扱う。
- 不要なビルドツール、フレームワーク、外部ライブラリを追加しない。
- モバイル表示を優先し、ボタンや入力欄はタップしやすくする。
- 診断ロジックは原則 `script.js` に集約する。
- 文言変更と判定ロジック変更は、できるだけ意図が分かる形で分ける。
- ユーザー入力をHTMLへ出す場合は、必ずエスケープする。
